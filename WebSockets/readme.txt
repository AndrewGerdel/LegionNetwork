Websockets are used for node-to-node communication, such as messge broadcasts, block broadcasts, etc.

This is different than the WebServices that are API endpoints exposed by each node to allow external applications to interact with the network. 